import styled from 'styled-components';

const DrawerFormContent = styled.div`

.form-content{
    padding: 25px 15px 30px 15px;
}
.pointer{
    cursor: pointer;
}

`;

export default DrawerFormContent;